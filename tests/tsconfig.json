// Fake config to satisfy the vscode intellisense
{
    "extends": "../tsconfig.json",
    "compilerOptions": {
        "types": ["node", "jest"],
        "allowJs": true,
        "sourceMap": true,
    },
    "include": ["./**/*", "../src/**/*"],
    "exclude": ["node_modules", "../*.config.*"],
}
